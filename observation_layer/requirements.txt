# WE_MMA_Receiver 依賴套件
# 接收端程式依賴

# 基本套件
numpy>=1.20.0
opencv-python>=4.5.0
Pillow>=8.0.0

# 如果需要動作識別功能，需要以下套件
# 可從 we_mma_2 的 requirements.txt 參考完整列表

# 注意：此套件依賴 we_mma_2 的模組
# 執行前請確保 we_mma_2 可正確導入
