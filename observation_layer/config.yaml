# =============================================================================
# WE_MMA_Receiver 配置檔
# =============================================================================
# 此檔案控制感知層的所有設定
# 修改後需重新啟動程式才會生效
# =============================================================================

# 是否開啟除錯模式
debug: true

# 房間名稱（用於記憶層標記）
room_name: "Living Room"

# -----------------------------------------------------------------------------
# GUI 介面設定
# -----------------------------------------------------------------------------
gui:
  window_title: "WiseEye2 Visualizer + MMAction2"
  window_size: "1200x900"
  canvas_width: 640
  canvas_height: 480
  action_panel_height: 150

