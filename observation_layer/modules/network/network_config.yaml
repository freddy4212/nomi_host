# =============================================================================
# Network Module Configuration (網路接收模組設定)
# =============================================================================

network:
  mode: "server"  # "server" 或 "client"
  host: "0.0.0.0"
  port: 9527
  buffer_size: 65536
  reconnect_interval: 2.0
