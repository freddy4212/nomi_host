# WE_MMA_Sender 依賴套件
# 發射端程式依賴

# 基本套件
numpy>=1.20.0
opencv-python>=4.5.0
Pillow>=8.0.0
pyserial>=3.5

# YOLO-Pose（Webcam 模式需要）
ultralytics>=8.0.0
# ReID 特徵提取（可選，未安裝時使用模擬模式）
# torch>=1.9.0
# torchvision>=0.10.0
# torchreid  # 安裝方式：pip install torchreid
# 或使用 TFLite：
# tensorflow>=2.5.0