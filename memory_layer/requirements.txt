# Home Agent Memory Layer dependencies
# 記憶層專用 PostgreSQL + TimescaleDB + pgvector

# PostgreSQL 連接器（必要）
psycopg2-binary>=2.9.0

# 可選：非同步 PostgreSQL（未來可能使用）
# asyncpg>=0.28.0

# 注意：
# - 需要先安裝 PostgreSQL 15+
# - 需要啟用 TimescaleDB 擴展
# - 需要啟用 pgvector 擴展
# 詳見 README.md 安裝說明
