# NOMI Home Agent Dependencies

# Core
numpy>=1.20.0
opencv-python>=4.5.0
Pillow>=8.0.0

# Web Server & API
fastapi>=0.100.0
uvicorn>=0.20.0
websockets>=11.0

# Perception Layer (YOLO-Pose)
# ultralytics>=8.0.0

# Memory Layer (PostgreSQL + TimescaleDB + pgvector)
psycopg2-binary>=2.9.0

# Inference Layer (LLM)
python-dotenv>=1.0.0
PyYAML>=6.0.1
ruamel.yaml>=0.17.0  # 保留 config.yaml 註解/格式的往返讀寫（未安裝時自動退回 PyYAML）
google-genai>=1.0.0

# Device Simulator / Tools
pyserial>=3.5

# Optional / Deep Learning Frameworks (Uncomment if needed)
# torch>=1.9.0
# torchvision>=0.10.0
# tensorflow>=2.5.0

scipy
